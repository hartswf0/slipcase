ASSEMBLY APPENDIX — From Felicity to Protocol

The exact visible assembly prompt is preserved in:
- SLIPCASE_FINAL_PROMPT.txt
- _PROMPTS/001__SLIPCASE_ANCIENT_MASTER_15.0.txt
- index.html (embedded text)

Prompt byte identity note:
The build environment does not expose raw chat transport bytes. The preserved prompt is a transcription of the visible user turn. Its SHA-256 is computed in _SLIPCASE/VERIFICATION.txt, but byte-for-byte identity with platform transport is UNVERIFIED.

Claim/source machinery:
- felicity-to-protocol__SOURCE_MAP.txt

Return path:
- 000__RETURN_PATH.txt
