SLIPCASE — PORTABLE RESEARCH FIELD  (schema slipcase/15.55-AM)
======================================================================

STATUS: The assembly instrument was supplied by the researcher in the originating
conversation as a POML document titled "SLIPCASE — PORTABLE RESEARCH FIELD",
version 15.55-AM. Its full text is held in that conversation.

PRESERVED HERE: the instrument's operative structure, section by section, so that
another context can reconstruct the build without the originating conversation.

  <meta>        intent: enter a live research context, recover the work already
                happening, ship a portable field that survives separation,
                transmission, reconstruction, critique and later merge.
  <role>        lossless research compiler, research editor, small press.
  <stance>      TAKE A REAL STAB. BUILD, DO NOT DESCRIBE. The run ends with files.
  <model>       EVIDENCE (zettel/source/resource/prompt/appearance);
                FIELD (platform/link/concept/ghost/backlink);
                INTERPRETATION (moc/arrangement/trail/paper).
  <laws>        16 laws. Load-bearing ones: one zettel = one immutable payload =
                one root .txt card; the card is the payload; never rewrite an
                archived zettel; preserve ORIGINAL ID exactly; payload SHA-256 is
                machine identity; preserve every appearance/SOURCE/BibTeX/PLATFORM/
                LINKS/[[ADDRESS]]; every NEW zettel includes SOURCE URL but never
                alter an older payload to add one; resolve conservatively with no
                fuzzy matching; a GHOST is an unfinished address, not an error;
                derived views never alter evidence; missing and conflicting are
                legitimate research states; never invent provenance; compute every
                count and hash or mark PENDING/UNVERIFIED; never claim more
                verification than occurred; another model must be able to resume.
  <cards>       filename <ORDER>__<NAME>__<SOURCE>__<ORIGINAL-ID>__from-<ORIGIN>.txt
  <graph>       node and edge types; preserve literal text, source card, source
                field, ordinal, target, resolution state for every relation.
  <publication> the full file list this package implements.
  <index>       index.html is the sendable replication capsule; offline; file://.
  <reader>      quiet self-contained offline desk; no server/framework/CDN/fonts.
  <bibliography>three artifacts; classify UNIQUE/SHARED/…; never fabricate fields.
  <resources>   states LINK_ONLY/SNAPSHOT/LOCAL_FILE/PASTED/GENERATED.
  <paper>       WRITE ONE REAL PAPER. Argument first, metadata last. Do not
                summarize the archive or walk card by card. Every substantive
                claim traces PAPER CLAIM -> ZETTEL -> SOURCE -> CITEKEY.
                (This build wrote nine, one per cluster, at the researcher's
                explicit request in the same turn.)
  <provenance>  preserve the assembly prompt; making history records WHO/ORIGIN/
                CONTROL/UNTOUCHED/VERIFIED/UNVERIFIED; never "AI-assisted".
  <mark>        MARK.svg: tiny nonverbal disclosure mark, bottom-right, low ink.
  <return_path> checkpoint identity and rejoin phrase.
  <verification>compute rather than assume; expose gaps; never collapse to
                COMPLETE: YES.
  <process>     BOUND FIND CLASSIFY PRESERVE CITE RELATE RESOLVE ORGANIZE RENDER
                WRITE DISCLOSE VERIFY PACKAGE.
  <design>      Tufte (idea/evidence ink), Nelson (two-ended durable links),
                Luhmann (atomic thought, movable order, surprise).
  <final_test>  open the ZIP; open index.html alone; remove any derived view and
                the evidence still regenerates it.

DEVIATION FROM THE INSTRUMENT, DECLARED:
  The instrument asks for ONE paper. The researcher asked, in the same turn, for
  one paper PER CLUSTER. Nine were written. All other requirements are attempted.
  No PDF was compiled: no TeX engine exists in the build environment. .tex ships
  uncompiled and PDF STATUS is reported as PENDING, not as success.
