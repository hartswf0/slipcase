ARRANGEMENT

01. [[DEFAULT-IMAGES-CHI26-ROOT]] — A generator can fail to understand a prompt without failing to generate: semantic failure can appear as a polished image.
02. [[DEFAULT-IMAGES-CHI26-B]] — The paper observes default images in Midjourney, but it cannot yet locate the default inside the model.
03. [[DEFAULT-IMAGES-CHI26-B-1]] — The visible prompt need not be the generator's prompt: an invisible writer can stand between the user and the image model.
04. [[WORKWORDS-PROMPT-004]] — Once prompts are compiled, the writer no longer necessarily writes the words the model receives.
05. [[WORKWORDS-PROMPT-001]] — In prompt programming, specification and implementation can occupy the same sentence.
06. [[WORKWORDS-PROMPT-003]] — What a prompt leaves unsaid can function like borrowed code supplied by the model.
07. [[WORKWORDS-PROMPT-002]] — The prompt can keep working after the words have been removed.
08. [[WORKWORDS-PROMPT-005]] — Writing acquires a loss landscape when criticism is treated as a textual gradient.
09. [[WORKWORDS-PROMPT-006]] — The writer can stop breeding prompts and begin breeding the prompts that breed prompts.
10. [[WORKWORDS-PROMPT-007]] — Prompt language becomes literally hybrid when prose, control flow, and constraints share one executable object.
11. [[WORKWORDS-PROMPT-008]] — A prompt becomes a research object when it is treated as a variable instead of a message.
12. [[DEFAULT-IMAGES-CHI26-A-1]] — A generator can represent a concept early in denoising and then forget it before the image is finished.
13. [[DEFAULT-IMAGES-CHI26-F-2]] — A word can be meaningless to every human in the room and still be densely meaningful to the image model.
14. [[DEFAULT-IMAGES-CHI26-E-1]] — The instrument used to detect default-image convergence has a convergence pathology of its own: CLIP produces hubs.
